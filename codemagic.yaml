workflows:
  android-workflow:
    name: Android Build

    scripts:
      - yes | sdkmanager --licenses
      - pip install --upgrade pip
      - pip install buildozer cython
      - buildozer android debug

    artifacts:
      - bin/*.apk
