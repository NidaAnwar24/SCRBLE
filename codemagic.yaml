```yaml
workflows:
  android-workflow:
    name: Android Build

    scripts:
      - pip install --upgrade pip
      - pip install buildozer cython
      - buildozer android debug

    artifacts:
      - bin/*.apk
```
